# Sample dataset: sample_data

A small Seurat object with sample values.

## Usage

``` r
sample_data
```

## Format

A Seurat object with 2000 features across 1450 cells:

- counts:

  Count matrix

- meta.data:

  Metadata columns: sample, group, cell_type, batch

- umap:

  UMAP coordinates
